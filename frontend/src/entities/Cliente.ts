/**
 * Cliente é só dado: não tem regra nem invariante para proteger. Uma interface
 * basta — criar uma classe aqui seria cerimônia sem ganho.
 */
export interface Cliente {
  id: number
  nome: string
  email: string
}
